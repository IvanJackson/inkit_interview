[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.0"
pytest = ">=6.0"
werkzeug = ">=2.0"
python-dotenv = ">=0.15"

[dev-packages]

[requires]
python_version = "3.13"
